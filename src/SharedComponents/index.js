// export { default as Button } from './Button'; <- The library to be defined here
//import { Button, Card } from '../Shared +Components/shared'; <- Grab the object from an external .js
//import * as SharedComponents from '../components/shared'; <- Grab every object from the shared
